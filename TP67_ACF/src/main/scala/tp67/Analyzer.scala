package tp67

import utilities.Datatype._

// Import members of the generated object `tp67` so that functions like `san4` are in scope.
import _root_.tp67.tp67._

object Analyzer {
  def safe(p: statement): Boolean = {
    san6(p)
  }
}
